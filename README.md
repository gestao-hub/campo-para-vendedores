# campo-para-vendedores

This directory contains legacy project files and the active Lovable/TanStack app
as a nested repository at `campo-de-vendas/`.

The production source of truth is:

```text
/root/campo-para-vendedores/campo-de-vendas
```

Use the nested repository for the LP at `lp.baluhub.com.br`.
